# --- 環境設定 ---
# ボリュームのベースパス（評価環境では/home/login/dataとなる）
DATA_PATH=/home/nkannan/data

# --- ドメイン設定 ---
# WordPressサイトのドメイン名です。あなたのintra名に合わせてください。
DOMAIN_NAME=nkannan.42.fr

# --- MariaDB (MySQL) データベース設定 ---
# このセクションはMariaDBコンテナの初期化に使われます。

# MariaDBのrootユーザーのパスワードです。強力なものを設定してください。
# 例: openssl rand -hex 12
MYSQL_ROOT_PASSWORD=tsukuba

# WordPressが使用するデータベースの名前です。
MYSQL_DATABASE=wordpress

# WordPressがデータベースに接続するための専用ユーザー名です。
MYSQL_USER=nkannan

# 上記ユーザーのパスワードです。これも強力なものを設定してください。
# 例: openssl rand -hex 12
MYSQL_PASSWORD=tsuchiura

# --- WordPress 設定 ---
# このセクションはWordPressコンテナの初期化やwp-config.phpの生成に使われます。

# WordPressの管理者ユーザー名です。
WP_ADMIN_USER=nkannan_admin

# WordPress管理者のパスワードです。
WP_ADMIN_PASSWORD=akihabara

# WordPress管理者のメールアドレスです。
WP_ADMIN_EMAIL=nkannan@student.42.fr

# テスト用の一般ユーザーを作成する場合に設定します。（任意）
WP_USER=testuser
WP_USER_PASSWORD=passwd
WP_USER_EMAIL=test@example.com

# --- WordPressからデータベースへの接続情報 ---
# WordPressがDBに接続するために使用する情報です。
# docker-compose.ymlで定義したMariaDBのサービス名を指定します。
WP_DB_HOST=mariadb:3306

# 接続先のデータベース名。上記のMYSQL_DATABASEと同じ値を設定します。
WP_DB_NAME=wordpress

# 接続ユーザー名。上記のMYSQL_USERと同じ値を設定します。
WP_DB_USER=nkannan

# 接続パスワード。上記のMYSQL_PASSWORDと同じ値を設定します。
WP_DB_PASSWORD=tsuchiura
